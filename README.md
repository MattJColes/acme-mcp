# acme-mcp
A worked example of a secure FastMCP 3 server: JWT auth, per-group tool access, audit logging, multi-domain composition, and file delivery via S3 signed URLs. Companion to a coles.codes post.
