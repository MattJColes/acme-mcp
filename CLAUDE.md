See AGENTS.md for the repo guide: living specs in `openspec/specs/` are
anchored to code with ast-grep rules — resolve anchors before a change,
propose spec updates as diffs after, and keep every anchor resolving to
exactly one place.
